<%= form_for(@skill) do |f| %>
  <%= f.hidden_field :user_id, :value => @user.id %> <br />
  Name:<%= f.text_field :name %> <br />
  Rating:<%= f.text_field :rating %> <br />
  <%= f.submit %>

<% end %>

