<h2> Skills </h2>

<% @skills.each do |f| %> 
  user_id <%= f.user_id %>
  name <%= f.name %>
  <%= link_to "edit", edit_skill_path(f) %>
  <%= link_to "Delete", skill_path(f), :method => "DELETE" %> 
  <%= link_to 'show', skill_path(f) %><br/>

<% end %> 

<%= link_to "new", new_skill_path %>
